package TetrisGame;

import javafx.scene.canvas.GraphicsContext;

public enum Square {
    I, J, L, O, S, T, Z;

    public static void draw(double gx, double gy, Square type, GraphicsContext graphicsContext){

    }
}
