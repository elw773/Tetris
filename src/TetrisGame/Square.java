package TetrisGame;

import javafx.scene.canvas.GraphicsContext;

public enum Square {
    I, J, L, O, S, T, Z, NONE;

    public static void draw(double gx, double gy, double squareSize, Square type, GraphicsContext graphicsContext){

    }
}
